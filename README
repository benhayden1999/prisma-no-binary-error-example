see the backend/index.ts file. The user should be typed but it is not. Got some weird type on the findUniqueOrThrow when hovering.

- adding @prisma/client into the packages/db deps fixes everything just fine but as soon as you remove types break break.

- im fairly rookie and i scoured the internet for guidance but found none. - I am trying run prisma on lambda basically

- I followed the prisma guide here for non-rust setup: https://www.prisma.io/docs/orm/prisma-client/setup-and-configuration/no-rust-engine
